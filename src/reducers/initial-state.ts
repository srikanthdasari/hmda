import IStoreState from '../store/IStoreState';

const defaultState : IStoreState = {
    errorObj: null,
    larObj: null,
    location:null,
    rootObj :  null,
    stage: null    
};

export default defaultState;    